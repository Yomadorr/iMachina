	
/*
	$(document).ready( function() { resizeContentContainer();} );
	$(window).resize( function()  { resizeContentContainer(); } );
	
	// resize container
	function resizeContentContainer()
	{
		var headerSize=200;
		var winHeight=$(window).height();
		var containerHeight=$('#containertosscontent').height();

		if ((containerHeight+headerSize)<winHeight)
		{
			$('#containertosscontent').height(winHeight-headerSize);
		}
	}
	*/